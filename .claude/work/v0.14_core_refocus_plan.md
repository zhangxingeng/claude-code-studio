# v0.14 Core-Refocus Round — Living Plan

Status: **in flight** (updated 2026-07-16). The round's manager keeps this current; a fresh agent
resuming after a compact reads this file first, then `project_docs/roadmap.md` Phases 18–20 for the
shipped detail. Branch: **`v0.14-core-refocus`** (ccdeck, NOT pushed). Sibling repo:
**https://github.com/zhangxingeng/prompt-compose** (public, pushed).

## Mission (founder brief, 2026-07-16)

CC Deck refocuses on two core features — **search** and **edit/share** — everything else is
subtraction. The Prompt Library splits into its own product. A full quality/slop sweep runs AFTER
the cuts (no point auditing deleted code). The founder deliberately did not name the known bugs:
the bet is that a clean codebase makes them obvious — the sweep should surface them. Multi-agent
posture: manager orchestrates; opus builds; sonnet reads; every dispatch names model + agent type;
iterative-teammate gates (plan → approve → implement + commit → lead merges).

## Founder decisions (settled — do not relitigate)

| Decision | Call |
|---|---|
| Search scope | Messages only (user/assistant). Filters = date + project. Sort dropdown, source chips, tool-name filter: cut. |
| Semantic CHAT search | **Rejected outright** — "keyword is good enough… don't even try to build it in the next round." Recorded in roadmap Future-ideas (~line 820). |
| Semantic match in Prompt Compose | **Keep** (offline = user data never uploaded, not no model download). NEW ask: add a **similarity threshold** so the match panel drops low-scoring snippets instead of always showing the whole library reordered under a query. |
| Terminal launch / fork | Cut launcher whole. Resume/fork = copyable facts (`cd '<cwd>' && claude --resume '<id>'`, path, id) in `ResumeMenu.svelte`. Fork keeps truncated-duplicate-jsonl mechanism. |
| Provider profiles (#21) | Cut entirely. Release notes: users may delete `~/.claude/.ccstudio-providers-plaintext.json` (holds keys; never print contents). |
| Settings editor | Cut completely (third and final disposition of #18/#20), backend included. |
| Export | HTML stays untouched (core). PDF added via `window.print()` + print stylesheet; prints in current theme (founder may want light forced — feel-check). |
| Chat editing + single-slot backup | Keep (redact-before-share is the point). |
| Split repo | `prompt-compose` (founder-chosen name), public, own Tauri app, prompts view REMOVED from ccdeck. Contracts renamed "Prompt Compose — …" and moved. |
| Compose model (phase 3) | Inserted snippets become **editable tinted text** (provenance highlight, freely editable, NO link back to library file — divergence defect can't return because there's no linked state). Replaces chips/popup machinery. |
| No migrations | Cuts need none (appconfig ignores stale keys; search schema-fingerprint forces one auto index rebuild). `~/.ccdeck/prompts-state.json` + embeddings cache left inert in ccdeck; founder hand-migrates (population of one). |

## State of work

**DONE, merged on `v0.14-core-refocus`, all green** (svelte-check 0/0, cargo 91, smoke incl. 12 new
browse-load assertions):
- #34 cuts + #36 PDF (net −6,764 lines) — roadmap Phase 18.
- #35 search narrowing (auto-rebuild proven by `schema_fingerprint_differs_from_old_shape`).
- #37 progressive loading (stat-only stubs → streamed enrichment, cleanup folded into the walk,
  browse windowed at 100) — roadmap Phase 19.
- Lead reconcile commit `e2290d6`: search project-filter chips self-heal from enrichment
  (`setProjectOptions` in `search.svelte.ts`, `$effect` in BrowseView).

**DONE — split phase 2** (commits `17b35c6`/`753f085`/`7bb9e80` on the feature branch): prompt tree
fully out of ccdeck (frontend + `src-tauri/src/prompts/` + 10 commands + 7 embed-chain crates +
dialog plugin), ARCHITECTURE/README/roadmap Phase 20 updated. All four gates green. Phase 1 (repo
scaffold + move, 4 commits pushed to prompt-compose) accepted earlier. Lead follow-up commit fixed
README's remaining stale claims (version-history/undo, sub-agent threads, tool-name/type filters,
the whole settings-editor section → replaced with an export/share section) and removed 2 more
orphaned screenshots.

**IN FLIGHT:** nothing — clean compact point (2026-07-16).

**QUEUED (in order):**
1. Phase 3 gate (in prompt-compose; dispatch a fresh opus teammate with the spec below if the split
   teammate is unreachable): editable-tinted-text compose model (replaces chips/popup — see
   Decisions); the founder's similarity threshold (match panel drops hits below a similarity floor
   under a non-empty query instead of showing the whole library reordered); prune dead ccdeck CSS
   in `app.css`; rename contract doc titles to "Prompt Compose — …"; confirm GitHub Actions CI ran
   green on the pushed commits.
3. **Quality/slop sweep** — scan-seeded fix campaign (`fix_campaign_manager_protocol` adaptive
   variant) over ccdeck src/ + src-tauri/ and prompt-compose: fix-local/escalate-wide, region-grouped
   fix waves, watch for the founder's unnamed bugs. Also prune stale branches (0.13 leftovers:
   `lane-*`, old `worktree-agent-*`) and stale worktrees.
4. Docs sync (doc-maintainer): ARCHITECTURE.md sweep vs final tree; CONTRIBUTING project-layout
   line (drops settings/prompts); README (phase 2 partially covers).
5. Release prep 0.14.0 — use the `cut-release` skill (version-bump file set + lockfile + CI-on-tag
   behavior). Release notes must carry: index auto-rebuild note, plaintext-key-file deletion note,
   orphaned prompts-state note, features-removed list.
6. Founder feel-check (blocking release): resume popover + fork-then-copy, OS Save-as-PDF dialog
   (unverified headless; current-theme printing), first-paint on real large history, prompt-compose
   app end-to-end.

## Process facts a fresh agent needs

- `gh` GraphQL is broken on this machine (spurious 401): `gh issue view/create/comment` fail —
  **use `gh api repos/zhangxingeng/ccdeck/...` (REST)**, which works.
- Worktree-reclamation quirk bit again: the search teammate's idle worktree (no commits at plan
  gate) was reclaimed; on resume it landed in the MAIN checkout and committed directly to the
  feature branch (content verified fine). Cuts/loading teammates' worktree branches
  (`worktree-agent-afbe4607955903c5d`, `worktree-agent-a692a77e99ac5fc5f`) are merged — safe to
  delete at cleanup.
- Issues this round: ccdeck #34 #35 #36 #37 (+ #32 split, decision comment on it). Close via the
  release/integration commits, not before.
- check_cmd: `pnpm check && cargo test --lib --manifest-path src-tauri/Cargo.toml && pnpm run test:smoke`.
